Mission Bridge

This package implements mission reporting helpers (image capture, path recorder, mission trigger).

Dependencies you may need to install on Ubuntu:

sudo apt update
sudo apt install -y ros-humble-rosbridge-server

Also ensure `rosbridge_suite` is available in your ROS environment if you plan to use the included WebSocket publishing.
